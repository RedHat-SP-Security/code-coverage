summary: Setup task for enabling measuring code test coverage
require+:
  - fapolicyd
  - library(epel/epel)
  - library(rpm/snapshot)
  - library(ControlFlow/Cleanup)
  - rpm-build
recommend+:
  - lcov
duration: 15m
